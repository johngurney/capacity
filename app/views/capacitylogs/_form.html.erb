<%= form_with(model: capacitylog, local: true) do |form| %>
  <% if capacitylog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(capacitylog.errors.count, "error") %> prohibited this capacitylog from being saved:</h2>

      <ul>
      <% capacitylog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="field">
    <%= form.label :capacitycode %>
    <%= form.number_field :capacitycode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
